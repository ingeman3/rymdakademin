# Copy to .env before running `docker compose up`.
# The compose file refuses to start if any of these are missing.
# CHANGE THESE VALUES for anything other than local development.
DB_NAME=rymdakademin
DB_USER=rymd_user
DB_PASSWORD=change-me
